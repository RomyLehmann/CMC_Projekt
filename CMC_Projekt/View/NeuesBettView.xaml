<UserControl x:Class="CMC_Projekt.View.NeuesBettView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="6"
                Background="White" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="180" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Neues Bett hinzufügen"
                               FontSize="20"
                               FontWeight="Bold"
                               Margin="0,0,0,20"/>

                    <TextBlock Text="Zimmernummer" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtZimmerNummer"
                             Height="34" FontSize="14" Padding="10,6"
                             Margin="0,0,0,14" />

                    <TextBlock Text="Bettnummer" FontWeight="Bold" Margin="0,0,0,6"/>
                    <TextBox x:Name="txtBettNummer"
                             Height="34" FontSize="14" Padding="10,6"
                             Margin="0,0,0,14" />

                    <TextBlock Text="Status" FontWeight="Bold" Margin="0,0,0,6"/>
                    <ComboBox x:Name="cmbStatus"
                              Height="34" FontSize="14" Padding="10,6"
                              Margin="0,0,0,14">
                        <ComboBoxItem Content="Frei" IsSelected="True"/>
                        <ComboBoxItem Content="Belegt"/>
                    </ComboBox>

                    <TextBlock Text="Wartung" FontWeight="Bold" Margin="0,0,0,6"/>
                    <ComboBox x:Name="cmbWartung"
                              Height="34" FontSize="14" Padding="10,6"
                              Margin="0,0,0,14">
                        <ComboBoxItem Content="Sauber" IsSelected="True"/>
                        <ComboBoxItem Content="Nicht sauber"/>
                    </ComboBox>

                    <TextBlock x:Name="txtInfo"
                               Text=""
                               Foreground="DarkGreen"
                               FontWeight="Bold"
                               Margin="0,10,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <Button Content="Speichern"
                            Width="160" Height="40"
                            FontSize="14" FontWeight="Bold"
                            Background="#27AE60"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="Save_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

